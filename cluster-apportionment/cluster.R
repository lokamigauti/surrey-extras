library(tidyverse)

import_dataset <- function(data_path) {
  #` Import colocation dataset into a tibble
  #`

}

interpolation <- function(y, x, target_x, method, ...) {
  #` Interpolate dataset and return new sampling
  #`
}

main <- function() {

}

if (!interactive()) {
  main()
}